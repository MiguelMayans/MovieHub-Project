import styles from "./MovieDetailModal.module.css"

type Props = {}

const MovieDetailModal = (props: Props) => {
    return (
        <div>MovieDetailModal</div>
    )
}

export default MovieDetailModal